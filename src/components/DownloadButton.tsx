import React from 'react';interface Props {  data: string[][];  fileName: string;}const DownloadButton: React.FC<Props> = ({ data, fileName }) => {  const downloadCsv = () => {    const csv = data.map(row => row.join(',')).join('\n');    const blob = new Blob([csv], { type: 'text/csv' });    const url = window.URL.createObjectURL(blob);    const a = document.createElement('a');    a.href = url;    a.download = fileName;    a.click();    window.URL.revokeObjectURL(url);  };  return (    <button onClick={downloadCsv} disabled={data.length === 0}>      Download YNAB CSV    </button>  );};export default DownloadButton;